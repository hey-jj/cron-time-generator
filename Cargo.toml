[package]
name = "cron-time-generator"
version = "0.1.0"
edition = "2021"
description = "generate cron expression strings via a fluent builder API"
license = "MIT"
repository = "https://github.com/hey-jj/cron-time-generator"
readme = "README.md"

[dependencies]
